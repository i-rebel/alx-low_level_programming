#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	int x;

	x = 01;

	for (x = 01; x < 100; x++)
	{
		printchar(x)
			if (x < 99)
			{
				printchar(',');
				printchar(' ');
			}
	}
	printchar('\n');
	return (0);
}
