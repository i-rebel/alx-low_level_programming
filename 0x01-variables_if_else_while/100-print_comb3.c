#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	int x;

	x = 01;

	for (x = 01; x < 100; x++)
	{
		putchar(x)
			if (x < 99)
			{
				putchar(',');
				putchar(' ');
			}
	}
	putchar('\n');
	return (0);
}
