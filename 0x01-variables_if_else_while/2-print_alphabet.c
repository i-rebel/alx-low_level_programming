#include <stdio.h>

/**
 * main - prints the alphabet in lowercaseprints the alphabet in lowercase
 *Return: returns 0 (Success)
 */

int main(void)
{
	char alphabet;

	while (alphabet++)
	{
		putchar("alphabet");
		putchar(' ');
	}
	return (0);
}
