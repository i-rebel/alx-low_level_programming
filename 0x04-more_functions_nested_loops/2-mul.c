#include "main.h"

/**
 * mul - multiplies two numbers
 * @a: first integer to be multiplies
 * @b: seocond integer to be multiplied
 * Return: Always 0
 */

int mul(int a, int b)
{
	int times;

	times = a * b;
	return (times);
}
