#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @int: character to be checked
 * Return: Always 0
 */

int _abs(int)
{
	int i;
	int j = abs(i);

	_putchar(j);
	return (0);
}
