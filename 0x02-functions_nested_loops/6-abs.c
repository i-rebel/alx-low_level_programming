#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @i: character to be checked
 * Return: Always 0
 */

int _abs(int i)
{
	if (i > 0)
{
	int abs_val = i * 1;

	return (abs_val);
}
	return (i);
}
